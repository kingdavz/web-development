/**
 * a module that lists odd numbers
 */

function getOdd(){
    let  num = 1;

    while (num <= 50) {
        console.log(num);
        num += 2;
    }
}

getOdd()

/**
 * implement nqueens algorithm
 * create a function that can convert binary to decimal and vice verrsa
 */